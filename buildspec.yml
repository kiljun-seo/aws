version: 0.2

#env:
  #variables:
     # key: "value"
     # key: "value"
  #parameter-store:
     # key: "value"
     # key: "value"

phases:
  install:
    commands:
      - npm install
  pre_build:
    commands:
      - npm run lint
      # - command
  build:
    commands:
      - npm run build
      # - command
  post_build:
    commands:
      #  - command
      # - command
artifacts:
  files:
    - dist
    # - location
  #name: $(date +%Y-%m-%d)
  #discard-paths: yes
  #base-directory: location
#cache:
  #paths:
    # - paths